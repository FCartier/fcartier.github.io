// `app/project/page.tsx` is the UI for the `/project` URL
const ProjectsPage = () => {
    return (
      <div>
        {/* Project Content */}
      </div>
    )
  }
  
  export default ProjectsPage;
  